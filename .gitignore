node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# electron-builder output and the native helper staged into the app bundle.
release/
apps/*/resources/

# Image artefacts written by `pnpm --filter @pilot/observation demo:image`.
# The demo regenerates them; the *approval* lives in image-demo.test.ts as
# digests of the decoded pixels, so no binaries are committed.
packages/observation/artifacts/

# Transient git worktrees used by parallel agent sessions.
.claude/worktrees/

# SwiftPM build output (packages/platform-mac/native). `Package.resolved` is
# deliberately not ignored: it must be committed once the helper gains
# dependencies.
.build/
.swiftpm/
